# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Raw Figma exports (~60 MB) — source material, not part of the build.
# Assets actually used by the site live in src/assets/design/.
Design PNG/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment
.env
.env.local

# Old local blog preview output; drafts are now served by the dev middleware
# in vite.config.js, so nothing is written here any more.
public/_preview/

# Firestore exports — contain chat transcripts and inbox messages
backup/

# Course source material used by the blog-post skill to pick topics.
# Untracked pending a decision on whether this repo is public: the
# documents carry pricing and business model detail.
mXL document for blog/
